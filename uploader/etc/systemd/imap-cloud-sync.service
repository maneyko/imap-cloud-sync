[Unit]
Description=Sync IMAP mailboxes to S3 (imap-cloud-sync)
After=network-online.target

[Service]
Type=oneshot
User=imap-cloud-sync
ExecStart=/opt/imap-cloud-sync/uploader/main.py
# main.py exits 128+signum after checkpointing, so a `systemctl stop` is not a failure.
SuccessExitStatus=130 143
